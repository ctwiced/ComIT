let userCount = 100;

userCount += 5;

console.log(userCount);

userCount -= 3;

console.log(userCount);

userCount *= 2 ;

console.log(userCount);

let genderDivide = userCount / 2;

let template = `The number of users who identify as men is ${genderDivide} and the number of users who identify as women is ${genderDivide}`

console.log(template)