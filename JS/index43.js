const myText = 'game of thrones';
const firstChar = myText.charAt(0);
const secondChar = myText.charAt(5);
const thirdChar = myText.charAt(8);
const resultName = firstChar + secondChar + thirdChar;
const upperCase = resultName.toUpperCase();

console.log('The final result is: ' + upperCase)

