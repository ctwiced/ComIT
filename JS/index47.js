//I wanted to play around with functions, so I put this exercise within a function
function greeting() {
    const studentNames = 'Chris, Ezekial, Michelle, Franks, Steph';
    const nameArray = studentNames.split(','); 

    console.log(nameArray);
}

greeting();